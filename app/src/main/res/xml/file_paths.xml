<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Recordings live in filesDir/recordings; expose them for sharing via FileProvider. -->
    <files-path name="recordings" path="recordings/" />
</paths>
